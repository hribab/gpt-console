      // const port = 8085;
      // console.log("Please go to https://gptconsole.ai/login?type=terminal");

      // // Handle the login callback
      // const handleLoginCallback = (req, res) => {
      //   res.statusCode = 200;
      //   res.setHeader('Content-Type', 'text/plain; charset=utf-8'); // Update the Content-Type header

      //     // Parse parameters from the URL
      //   const urlParams = new URL(req.url, `http://${req.headers.host}`).searchParams;
      //   const name = urlParams.get('name');
        
      //   // Display the login message in the console
      //   console.log(`Successfully logged in as: ${name}`);
      //   res.end("You are successfully logged in!");
        
      // };

      // // Create an HTTP server to handle the login callback
      // const server = http.createServer(handleLoginCallback);
      // server.listen(port, () => {
      //   console.log(`Login server listening on port ${port}`);
      // });