async function stopPixie(input, callback) {
    try {      
       console.log("=====stop====")
    
      } catch (err) {
        return `An error occurred during API call: ${err}`;
      }
}
    
module.exports = {
    stopPixie
}