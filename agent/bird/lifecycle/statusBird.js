async function statusBird(input, callback) {
    try {      
       // console.log("=====status====")

        } catch (err) {
        return `An error occurred during API call: ${err}`;
        }
    }

module.exports = {
    statusBird
}